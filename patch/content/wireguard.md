## WireGuard

Most of the time, we'll be playing using the WireGuard VPN. Windows users can  Linux users can use the launcher as a GUI or [wireguird](https://github.com/UnnoTed/wireguird).

If you don't have a config file, head to [the configuration page here](https://patrick.munni.ch). The login password is `ACBrotherhood`.

